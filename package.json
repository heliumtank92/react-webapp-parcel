{
  "name": "react-webapp-parcel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "analyze": "pnpm run clean-build && parcel build public/index.ejs --reporter @parcel/reporter-bundle-analyzer && pnpm run move-assets",
    "start": "pnpm run clean-build && parcel public/index.ejs --open && pnpm run move-assets",
    "build": "pnpm run clean-build && parcel build public/index.ejs && pnpm run move-assets",
    "move-assets": "cp -r public/assets dist/assets",
    "clean-build": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.8.3",
    "@parcel/packager-raw-url": "^2.8.3",
    "@parcel/packager-xml": "^2.8.3",
    "@parcel/reporter-bundle-analyzer": "^2.8.3",
    "custom-env": "^2.0.1",
    "parcel": "^2.8.3",
    "parcel-config-pwa-manifest": "^0.1.2",
    "parcel-transformer-ejs": "^1.0.1"
  },
  "alias": {
    "process": {
      "global": "{}"
    }
  }
}
