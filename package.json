{
  "name": "react-webapp-parcel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel public/index.html --lazy --open chrome --port 3000",
    "build": "parcel build public/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "parcel": "^2.8.3"
  },
  "alias": {
    "process": {
      "global": "{}"
    }
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
